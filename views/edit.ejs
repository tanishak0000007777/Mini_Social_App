<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen py-10 px-4">
    <div class="max-w-3xl mx-auto">
            <!-- Logout -->
        <div class="flex justify-end mb-6">
            <a href="/logout" class="bg-red-500 hover:bg-red-600 px-4 py-2 text-sm rounded-md transition">
              Logout
            </a>
          </div>
      
          <!-- Greeting -->
          <h2 class=" mb-6">You can edit your post</h2>
      
          <!-- Post Form -->
          <form action="/edit/<%= post._id %>" method="post" class="mb-12">
            <textarea
              name="content"
              placeholder="What's on your mind?"
              required
              class="w-full h-24 p-3 rounded-md bg-zinc-800 border border-zinc-700 outline-none resize-none mb-4 focus:border-blue-500"
            ><%= post.content %>
            </textarea>
            <input
              type="submit"
              value="Edit Post"
              class="px-6 py-2 bg-yellow-500 text-black hover:bg-yellow-700 rounded-md text-sm transition cursor-pointer"
            />
          </form>
      
          <!-- Posts Section -->
    </div>
</body>
</html>